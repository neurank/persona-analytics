# API base URL for dev (e.g. http://localhost:5123)
VITE_DEV_API_URL=http://localhost:5000

# API base URL for prod (empty = relative /api for nginx proxy)
# VITE_PROD_API_URL=

# Admin key for dev only (Persona X-Admin-Key). Prod uses nginx to add it.
# VITE_DEV_ADMIN_KEY=
